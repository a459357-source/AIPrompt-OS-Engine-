# V6.0 Visual Narrative System — Phase A Completion Report

**日期**: 2026-06-08  
**前置**: V6.0 Phase A Architecture APPROVED WITH DECISIONS（`304b7d3`）  
**范围**: Phase A only（未进入 Phase B Agnes / 未进入 V6.5 UI）

---

## 交付摘要

建立 **Shared System** 级 Visual Runtime Foundation：

- `VisualProvider` 抽象 + `StubVisualProvider` / `MockVisualProvider`
- `visual_registry.json` 用户运行时元数据（**不入 Git**）
- 四类资产 API：立绘 / 场景 / 世界地图 / 势力图
- 缓存落盘 `output/visual/`（**不进存档 slot**）
- Save/Load/Reset/New Story 接线

**未接** Agnes 真实 API、Event Director 自动生图、任何前端/UI。

---

## 新增文件

| 路径 | 说明 |
|------|------|
| `engine/visual/visual_registry.py` | 注册表 CRUD |
| `engine/visual/asset_manager.py` | 对外 API |
| `engine/visual/visual_provider.py` | `VisualProvider` + Stub/Mock |
| `engine/visual/agnes_visual_provider.py` | Phase B 占位（Phase A 调用即报错） |
| `engine/visual/visual_context.py` | Prompt Builder |
| `engine/visual/visual_cache.py` | 文件缓存 |
| `engine/visual/image_generation.py` | 生成编排 |
| `packaging/defaults/visual_registry.json` | 空模板 |
| `test_visual_phase_a.py` | Phase A 验收测试 |

---

## 评审裁决落实

| 裁决 | 实现 |
|------|------|
| Registry = User Runtime Data | `data/visual_registry.json` 在 `data/*` gitignore 内 |
| 图片不进存档 | snapshot 仅 `visual_registry` 元数据；无 PNG/Base64 |
| Agnes Phase B | `AgnesVisualProvider` 存在但 `AgnesNotConfiguredError` |
| 不挂 Event Director | 未修改 `run.py` / `event_director` |
| visual_theme 隔离 | `engine/visual/` 无 `adult_mode` / `experience_mode` / `visual_theme` |

---

## Phase A 验收（14/14）

| # | 标准 | 状态 |
|---|------|------|
| 1 | `visual_registry.py` | ✅ |
| 2 | `asset_manager.py` | ✅ |
| 3 | `VisualProvider` 抽象 | ✅ |
| 4 | character portrait | ✅ |
| 5 | scene image | ✅ |
| 6 | world map | ✅ |
| 7 | faction map | ✅ |
| 8 | Registry 持久化 | ✅ |
| 9 | save/load/reset | ✅ |
| 10 | ADR-001 | ✅ |
| 11 | 无 StoryPortrait/AdultPortrait 等 | ✅ |
| 12 | Shared System | ✅ |
| 13 | Registry 与 Save/Reset/New Story 兼容 | ✅ `test_save_slot_metadata_only` |
| 14 | 不引用 Mode Layer 字段 | ✅ `test_no_mode_layer_in_visual_code` |

---

## 自检

```bash
cd prompt-os-engine
python -m pytest test_visual_phase_a.py -q
python -c "from engine.visual import get_or_request_character_portrait"
```

**结果**: `14 passed`；import 冒烟通过。

---

## 原则确认

```text
Registry is State
Image is Cache
```

- 存档：`visual_registry` JSON 元数据（含 `uri`、`prompt_hash`、`provider`）
- 图片：`output/visual/` 可重建，读档后缺失可 lazy 再生成

---

## 已知限制（Phase B+）

- Agnes 真实 endpoint 未接
- 无 Visual Trigger（Event/Plot/Relationship 自动生图）
- 无 UI / Dashboard / Theme 展示
- `force_regenerate` 仅 API 参数，无 UI 暴露

---

## 评审请求

**Phase A 完成，请评审。未启动 Phase B（Agnes Integration）。**
