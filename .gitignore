__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── 运行时 data/（个人进度、密钥、日志，永不提交）──
data/*
!data/.gitkeep

# ── 运行时根目录 YAML ──
session_state.yaml
world_pack.yaml

# ── 输出与导出 ──
output/

# ── 打包产物 ──
dist/
build_pyinstaller/
PromptOS.spec
release/*.zip
release/PromptOS/
